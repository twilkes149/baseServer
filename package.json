{
  "name": "base-server",
  "version": "1.0.0",
  "description": "This is a base server, will read from a json file to determine tables in databases. Anything that extends this will already have routes for login, registration, and get user",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Tucker Wilkes",
  "license": "ISC",
  "dependencies": {
    "dotenv-safe": "^5.0.1",
    "express": "^4.16.3",
    "promise-mysql": "^3.2.1"
  }
}
